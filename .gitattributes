# Keep shell scripts and Unix config files LF, whatever the editor does on Windows.
*.sh        text eol=lf
*.conf      text eol=lf
*.service   text eol=lf
*.rules     text eol=lf

# Leave the hand-maintained source-of-truth file alone.
Manual-Direct-commands.txt -text
